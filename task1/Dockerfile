# Étape 1 : partir de l’image officielle Ubuntu la plus récente
FROM ubuntu:latest

# Étape 2 : mettre à jour la liste des paquets
RUN apt-get update

# Étape 3 : mettre à jour les logiciels installés
RUN apt-get upgrade -y

# Étape 4 : afficher "Hello, World!" quand le conteneur est lancé
CMD echo "Hello, World!"
